{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "license": "MIT",
  "devDependencies": {
    "@storybook/storybook-deployer": "^2.8.16",
    "turbo": "^1.8.3"
  }
}
