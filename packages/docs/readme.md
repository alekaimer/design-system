# Storybook

Command for Storybook instalation:
```
npx sb init --builder @storybook/builder-vite --type react --use-npm
```

Development:
```
npm run dev
```

Build:
```
npm run build
```

Lint (fix):
```
npm run build
```